[build-system]
requires = ["scikit-build-core>=0.10", "pybind11>=2.13"]
build-backend = "scikit_build_core.build"

[project]
name = "abicheck-lab-py"
version = "0.1.0"
requires-python = ">=3.9"

[tool.scikit-build]
wheel.packages = ["abicheck_lab_py"]
